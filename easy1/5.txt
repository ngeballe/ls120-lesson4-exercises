Question 5

Which of these two classes has an instance variable and how do you know?

class Fruit
  def initialize(name)
    name = name
  end
end

class Pizza
  def initialize(name)
    @name = name
  end
end

Answer:

The class "Pizza" is the one that has an instance variable (@name). You can tell because "@name" starts with an "@".

